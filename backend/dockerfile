# 1. 使用官方 Node 18 image
FROM node:18

# 2. 設定工作目錄
WORKDIR /app

# 3. 複製 package.json 和 package-lock.json（如果有）
COPY package*.json ./

# 4. 安裝依賴
RUN npm install

# 5. 複製其他程式碼
COPY . .

# 6. 暴露 port（假設你的 app.js 用 3000）
EXPOSE 3000

# 7. 容器啟動指令（開發用，熱更新 nodemon）
CMD ["npm", "run", "dev"]